@page "/"
@inherits HomePageBase

<PageTitle>HỆ THỐNG QUẢN LÝ THIẾT BỊ - Đăng nhập quản trị viên</PageTitle>

<main class="login-shell">
	<section class="login-wrapper">
		<header class="brand-header">
			<h1 class="brand-title">HỆ THỐNG QUẢN LÝ THIẾT BỊ</h1>
			<p class="brand-subtitle">Công ty CAE TECHNOLOGY</p>
		</header>

		<article class="login-card">
			<header class="login-head">
				<h2>Đăng nhập</h2>
				<p>Vui lòng nhập thông tin để truy cập hệ thống.</p>
			</header>

			@if (!string.IsNullOrWhiteSpace(StatusMessage))
			{
				<div class="status-banner @(IsError ? "error" : "ok")">@StatusMessage</div>
			}

			<EditForm Model="Model" OnValidSubmit="HandleSubmitAsync" FormName="LoginForm" method="post" class="login-form">
				<DataAnnotationsValidator />

				<div class="field-block">
					<label for="username">TÊN ĐĂNG NHẬP</label>
					<div class="input-wrap">
						<span class="material-symbols-outlined">person</span>
						<InputText id="username" @bind-Value="Model.Username" autocomplete="username" />
					</div>
					<ValidationMessage For="() => Model.Username" />
				</div>

				<div class="field-block">
					<label for="password">MẬT KHẨU</label>
					<div class="input-wrap">
						<span class="material-symbols-outlined">lock</span>
						<InputText id="password" @bind-Value="Model.Password" type="password" autocomplete="current-password" />
					</div>
					<ValidationMessage For="() => Model.Password" />
				</div>

				<button type="submit" class="login-btn" disabled="@Submitting">
					<span>@(Submitting ? "Đang xử lý..." : "Đăng nhập")</span>
					<span class="material-symbols-outlined">login</span>
				</button>
			</EditForm>

			<footer class="login-foot">
				<p>Phiên bản 2.4.0 - Enterprise Edition</p>
			</footer>
		</article>
	</section>
</main>
